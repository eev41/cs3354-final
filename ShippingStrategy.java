//Define the Interface: Create an interface named ShippingStrategy 
//with a single method: double calculate(double weight, double distance).

public interface ShippingStrategy {
    double calculate(double weight, double distance);
}
